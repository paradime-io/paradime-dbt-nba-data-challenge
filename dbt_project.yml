name: "nba"
version: "1.0.0"
config-version: 2

profile: "nba"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  +schema: raw_data


models:
  nba: 
    sources:
      +schema: staging
      +materialized: view
    example_transformations:
      intermediate:
        +schema: example_intermediate
        +materialized: view
      warehouse:
        +schema: example_warehouse
        +materialized: table
    transformations:
      intermediate:
        +schema: intermediate
        +materialized: view
      analytics:
        +schema: analytics
        +materialized: view